# Run order-based single-character edit

Updated: 2026-09-22T12:05:28.390Z
Workspace: /home/kyrie/cxprojects/SynSLP
Target agent: Codex (codex)

## Plan

# Goal

Fix the downstream character-mask selector so a **specific annotated character instance** is selected by `order`, then run exactly one minimal AnyText2 single-character edit on the server:

[
	ext{东泰168} ightarrow 	ext{东泰268}
]

Only the character instance `order=2` (human annotation: `1`) may be masked and regenerated.

This task answers one question:

> Can AnyText2 perform a strictly local one-character replacement on a tight-crop SLP when the edit region is reduced to one human-annotated character bbox?

Do not expand to multi-character editing, Font Mimic tuning, seed sweeps, or batch synthesis.

# Frozen source data

Canonical human annotation:

`annotations/character_annotations.jsonl`

Current verified record:

- image: `reference/easy&single&ng&nd&东泰168&8&1&T_20220519_11_29_59_740944.jpg`
- image size: `239 x 57`
- `order=0`: `东`, bbox `[2, 6, 47, 55]`
- `order=1`: `泰`, bbox `[56, 5, 103, 55]`
- `order=2`: `1`, bbox `[120, 3, 147, 52]`
- `order=3`: `6`, bbox `[157, 4, 184, 53]`
- `order=4`: `8`, bbox `[203, 4, 231, 53]`

The annotations are human GT and must not be modified by this task.

# Step 1 — Make instance/order selection canonical

Modify `scripts/generate_character_masks.py` minimally so downstream masks can be generated by exact annotation `order`.

Required behavior:

- add an explicit order-based selection interface, e.g.:
  ```bash
  --orders 2
  ```
- the mask generator must select only the annotation instance(s) whose `order` exactly matches the requested order(s);
- output mask dimensions must remain the original image dimensions;
- preserve the existing bbox convention `[x1,y1,x2,y2)`;
- the generated binary mask must be 255 inside selected bbox pixels and 0 elsewhere.

The existing character-value selector may remain only as a clearly documented convenience/legacy path if removing it would create unnecessary breakage. It must no longer be the canonical way to identify a specific character instance.

Important correctness rule:

[
	ext{character value} 
eq 	ext{character identity}
]

Add a focused regression test containing duplicate characters, e.g. two different `0` instances with different orders, and prove that selecting one order activates only that one bbox.

For the real `东泰168` annotation, verify:

```text
--orders 2
=> exactly one matched instance
=> text = "1"
=> bbox = [120, 3, 147, 52]
```

Expected original-resolution active area:

[
(147-120)(52-3)=27	imes49=1323 	ext{pixels}
]

so the original `239x57` mask should contain exactly 1323 active pixels if no padding is applied.

Fail rather than silently selecting by character text if order selection is ambiguous, duplicated, or missing.

# Step 2 — Prepare one official-faithful 512x512 letterboxed edit input

Do not stretch the `239x57` crop into `512x128`.

For this single diagnostic experiment, create an in-memory **aspect-ratio-preserving 512x512 black letterbox**:

1. scale the reference uniformly so its long side becomes 512;
2. center it on a `512x512` black canvas;
3. apply the exact same geometric transform to the order-2 binary edit mask using nearest-neighbor interpolation;
4. do not dilate, erode, blur, redraw, or otherwise enlarge the mask;
5. record the exact scale and padding offsets.

Save review copies of:
- the letterboxed reference;
- the letterboxed order-2 mask.

The only white mask region must correspond to the source glyph `1`.

# Step 3 — Run exactly one AnyText2 single-character edit on server

Server:

- host: `server-zyx`
- repo: `/mnt/data/zyx/SynSLP`

Before inference:
- ensure the latest code implementing order-based selection is available on the server using the repository's existing safe Git synchronization convention;
- use the already synchronized annotation artifact on the server;
- verify the server-side order-2 annotation is still `text="1"`, bbox `[120,3,147,52]`.

Inference target:

```text
masked source glyph: 1
replacement text:   2
expected full visual text after edit: 东泰268
```

Use the existing verified AnyText2 deployment and checkpoint. Do not modify AnyText2 source/model architecture.

Use:
- mode: `edit`;
- target text prompt for the masked region only: `"2"`;
- seed: `2026`;
- DDIM steps: `20`;
- strength: `1.0`;
- eta: `0.0`;
- sort priority: `↔`;
- `revise_pos=False`;
- `attnx_scale=1.0`;
- Font Mimic: **disabled for this experiment**;
- one generated sample only.

For prompt/CFG defaults:
- inspect the pinned official AnyText2 demo currently deployed;
- use the official/default text-quality positive and negative prompt settings where compatible;
- use the official/default CFG value rather than the earlier hand-written SLP-specific prompt if the latter is not an official default;
- describe the image content neutrally if an image prompt is required; do not use wording that strongly induces a conventional physical car-license-plate/frame prior.

Record the exact effective prompts and CFG value. Do not sweep them.

If `show_debug=True` can be enabled without changing generation behavior, enable it and save the useful debug conditioning output(s), especially glyph/position control evidence. Do not block the experiment if debug export is unavailable.

# Step 4 — Save minimal review evidence

Use a dedicated output directory such as:

`outputs/single_char_edit/dongtai168_order2_1_to_2/`

Required artifacts:

- `reference_original.*`
- `mask_order2_original.png`
- `reference_letterbox_512.png`
- `mask_order2_letterbox_512.png`
- `output_512.png`
- `output_crop_original_res.png`
- `comparison.png`

The final crop must reverse the letterbox transform, crop only the original SLP content region, and resize back to `239x57` for human comparison.

Record:
- annotation record/hash/path used;
- selected order/text/bbox;
- mask active-pixel count at original resolution;
- letterbox scale/padding;
- exact AnyText2 parameters;
- inference time;
- warnings/errors;
- output paths.

Do not OCR-score or automatically declare success.

Human review will judge only:

1. Did `1` become visually recognizable as `2`?
2. Did the unmasked `东泰`, `6`, and `8` remain visually preserved?
3. Was unnecessary regeneration outside the order-2 bbox substantially reduced?
4. Does the final crop still look like the same real SLP image rather than a newly invented plate?

# Stop condition

Stop immediately after this one `1 -> 2` output and review evidence are produced.

Out of scope:

- editing `泰 -> 航`;
- multi-character masks;
- Font Mimic experiments;
- prompt sweep;
- CFG sweep;
- strength sweep;
- seed sweep;
- mask padding/dilation experiments;
- OCR filtering;
- batch synthesis;
- automatic quality scoring;
- downstream SLPR training.

# Implementation contract

- Keep human annotations unchanged.
- Fix instance identity at the mask-selection layer, not by changing annotation labels.
- Keep changes minimal and reviewable.
- Add/adjust focused tests for exact order selection and duplicate-character safety.
- Reuse the existing verified AnyText2 deployment.
- Do not modify third-party AnyText2 architecture.
- Update `.ai-bridge/agent-status.md` with files changed, tests/checks run, exact server-side parameters, outputs, blockers, and review notes.
- Save the final diff to `.ai-bridge/implementation-diff.patch` when practical.
- Append notable events to `.ai-bridge/execution-log.jsonl` when supported.

## Implementation contract

- Work from this plan in small, reviewable steps.
- Keep edits scoped to the requested task and existing project conventions.
- Run focused verification before handing work back.
- Update .ai-bridge/agent-status.md with files touched, checks run, results, blockers, and review notes.
- Save the final review diff to .ai-bridge/implementation-diff.patch when practical.
- Append notable execution events to .ai-bridge/execution-log.jsonl when the implementation agent supports logging.
