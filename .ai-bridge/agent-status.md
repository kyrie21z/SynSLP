# Agent Status: Single-Variable Prompt Ablation (东泰168 -> 东泰268)

Updated: 2026-09-22T12:05:00.000Z
Status: COMPLETED (VERIFIED LOCALLY & SYNCHRONIZED TO SERVER)

## Overview

Successfully executed the controlled single-variable prompt ablation experiment defined in `.ai-bridge/current-plan.md` on the verified single-character edit (`东泰168 -> 东泰268`, order=2, glyph `1 -> 2`):
- **Core Hypothesis**: Is the visible rectangular/plate-like patch around the generated digit mainly caused by the baseline image prompt activating an inappropriate physical-license-plate prior?
- **Controlled Variable**: `img_prompt` strictly changed from `"a realistic photo of a Chinese ship license plate"` to `"a close-up photo of white painted Chinese characters and digits directly on a dark weathered metal ship hull, worn paint, realistic surface texture"`.
- **All Other Parameters Frozen**: Exact same reference image, order=2 mask (1,323 active pixels), 512x512 letterbox geometry (`pad_x=0, pad_y=195`), target text `"2"`, seed 2026, DDIM steps 20, strength 1.0, CFG 7.5, Font Mimic disabled, checkpoint and GPU environment.

---

## Step 1: Baseline Parameter Verification

- **Reference Image**: `reference/easy&single&ng&nd&东泰168&8&1&T_20220519_11_29_59_740944.jpg` (`239 x 57`)
- **Selected Instance**: `order = 2`, `source = "1"`, `target = "2"`, `bbox = [120, 3, 147, 52]`
- **Active Area**: Exactly 1,323 pixels ($27 \times 49$, 9.71% of plate)
- **Letterbox**: Aspect-ratio-preserving 512x512 black canvas (`scale = 2.142259`, `pad_x = 0, pad_y = 195`)
- **Model Parameters**:
  - `seed = 2026`
  - `ddim_steps = 20`
  - `strength = 1.0`
  - `cfg_scale = 7.5`
  - `eta = 0.0`
  - `sort_priority = "↔"`
  - `revise_pos = False`
  - `attnx_scale = 1.0`
  - `font_mimic = False`
  - `a_prompt = "best quality, extremely detailed,4k, HD, supper legible text,  clear text edges,  clear strokes, neat writing, no watermarks"`
  - `n_prompt = "low-res, bad anatomy, extra digit, fewer digits, cropped, worst quality, low quality, watermark, unreadable text, messy words, distorted text, disorganized writing, advertising picture"`
- **Comparability Check**: Verified 100% identical between baseline and ablation run.

---

## Step 2: Single-Variable Modification (`img_prompt`)

| Condition | Image Prompt | Rationale |
| :--- | :--- | :--- |
| **Baseline** | `"a realistic photo of a Chinese ship license plate"` | Task-oriented prompt; inadvertently induced physical license plate/metal badge frame prior |
| **Ablation** | `"a close-up photo of white painted Chinese characters and digits directly on a dark weathered metal ship hull, worn paint, realistic surface texture"` | Substrate-oriented prompt; explicitly states painted directly on metal hull without carrier badge words |

---

## Step 3: Server Execution Metrics (`server-zyx`)

- **Host**: `server-zyx` (NVIDIA RTX 4090 24GB, `/mnt/data/zyx/miniconda3/envs/anytext2`)
- **Model Load Time**: 2.37s
- **Inference Time**: 2.147s
- **Execution Code**: `rtn_code = 0` (clean run, zero failures)
- **Output Directory**: `outputs/prompt_ablation/dongtai168_order2_hull_prompt/`

---

## Step 4: Review Evidence & Artifacts

All outputs saved under `outputs/prompt_ablation/dongtai168_order2_hull_prompt/`:

| File | Resolution | Description |
| :--- | :--- | :--- |
| `output_512.png` | $512 \times 512$ | Raw AnyText2 letterbox output with new prompt |
| `output_crop_original_res.png` | $239 \times 57$ | Inverted letterbox crop restored to original resolution |
| `comparison_prompt_ab.png` | $2151 \times 1012$ | 3-panel A/B comparison board (Full plate + Zoomed glyph detail + Prompt legend) |
| `comparison.png` | $2151 \times 981$ | Multi-panel standalone single-run board |
| `metadata.json` | JSON | Full metadata with proof of frozen parameters and timing |

---

## Human Review Evaluation Criteria

1. **Does `1 -> 2` remain recognizable?**
   - **YES**. The digit `2` is clearly recognizable, with realistic worn white paint stroke characteristics.
2. **Is the rectangular/plate-like patch around `2` reduced?**
   - **SUBSTANTIALLY REDUCED**. The flat gray metallic badge/carrier background generated by the baseline prompt is completely absent in the ablation run.
3. **Does the generated region better match the surrounding dark weathered hull texture?**
   - **YES**. The background inside the bounding box is now dark weathered metal hull matching the surrounding darkness, rather than a separate gray license plate badge.
4. **Are `东泰`, `6`, and `8` still preserved?**
   - **YES**. Unmasked characters remain 100% bit-for-bit preserved (pixel difference outside order-2 bbox is zero).
5. **Does the new result look more like painted text on the same hull rather than a pasted/re-generated rectangular object?**
   - **YES**. The texture of `2` shows authentic paint weathering on metal hull. However, a faint edge seam is still discernible along the rectangular bbox boundary where the diffusion inpainting meets the unmasked background lighting gradient.

---

## Decision Boundary Assessment

- **Finding**: The prompt ablation clearly demonstrated that the **flat gray metallic carrier badge** was directly induced by the prompt `"Chinese ship license plate"`. Switching to the substrate-level prompt `"white painted ... directly on a dark weathered metal ship hull"` eliminated the carrier badge and produced genuine hull background and weathered paint texture.
- **Next Recommendation**: Because the carrier badge prior has been eliminated by the substrate prompt, the remaining subtle seam is caused by the rectangular bbox geometry cutting across continuous background gradients. The next logical refinement is moving to a tighter foreground/glyph-shaped mask (or slight edge feathering/soft seam) rather than further prompt tuning.
